{
  "type": "mysql",
  "host": "${DB_HOST}",
  "port": "${DB_PORT}",
  "username": "${DB_USERNAME}",
  "password": "${DB_PASSWORD}",
  "database": "${DB_DATABASE}",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true
}
