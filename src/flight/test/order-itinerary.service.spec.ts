import { Test } from '@nestjs/testing';
// import { UserCalculateService } from './user-calculate.service';

describe('UserCalculateService', () => {
  // let service: UserCalculateService;

  beforeEach(async () => {
    // const moduleRef = await Test.createTestingModule({
    //   providers: [UserCalculateService],
    // }).compile();
    // service = moduleRef.get<UserCalculateService>(UserCalculateService);
  });

  describe('calculateAge', () => {
    it('should return the correct age', () => {
      // const birthDate = new Date('1990-01-01');
      // const result = service.calculateAge(birthDate);
      // expect(result).to.equal(32);
      expect(true).toEqual(true);
    });
  });
});
